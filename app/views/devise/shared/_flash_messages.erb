<!-- フラッシュメッセージを表示 -->
<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type == 'danger' ? 'danger' : message_type %>" role="alert">
    <i class="fas fa-exclamation-triangle me-2"></i>
    <%= message %>
  </div>
<% end %>
